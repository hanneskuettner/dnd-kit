{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency 15",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish",
    "version-packages:beta": "changeset version --snapshot beta",
    "release:beta": "changeset publish --snapshot --tag beta",
    "storybook": "node scripts/storybook.mjs",
    "storybook:react": "turbo run dev --filter=@dnd-kit/stories-react",
    "storybook:host": "turbo run dev --filter=@dnd-kit/stories"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.29.2",
    "prettier": "^3.1.1",
    "concurrently": "^9.1.2",
    "turbo": "^2.5.2",
    "wait-on": "^8.0.3"
  },
  "packageManager": "bun@1.1.12",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "name": "dnd-kit-experimental"
}
